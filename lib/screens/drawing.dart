import 'package:flutter/material.dart';

class DrawingPage extends StatelessWidget {
  static String id = 'drawing_page';

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Center(child: Text('TEKENEN')),
      ),
    );
  }
}
