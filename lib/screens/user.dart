import 'package:flutter/material.dart';

class UserssPage extends StatelessWidget {
  static String id = 'users_page';

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Center(child: Text('GEBRUIKER')),
      ),
    );
  }
}
